import { createApp } from "./createApp";
import { h } from "./vnode";
import { onMounted, getCurrentInstace } from "./component";
import { inject, provide } from "./injecct";

export { createApp, h, onMounted, getCurrentInstace, inject, provide };
